# 🏦 Bank Management System

A full-stack **Bank Management System** built using **Spring Boot** for the backend and **React + Redux** for the frontend.  
The application supports secure authentication, account management, transactions, and payment history.

## 📂 Project Structure

- `banking_backend/` — Java Spring Boot backend (REST API, business logic, database)
- `banking_frontend/` — React frontend (user interface)

## ⚙️ Tech Stack

### Backend

- Java
- Spring Boot
- JWT Authentication
- Maven
- MySQL / PostgreSQL

### Frontend

- React
- Redux
- Material UI
- REST APIs

## 🚀 Getting Started

### Backend

1. Navigate to ` cd banking_backend/`
2. Build and run with Maven:
   ```bash
   mvn spring-boot:run
   ```
3. The backend will start on [http://localhost:8080](http://localhost:8080)

### Frontend

1. Navigate to `cd banking_frontend/`
2. Install dependencies:
   ```bash
   npm install
   ```
3. Start the frontend:
   ```bash
   npm start
   ```
4. The frontend will start on [http://localhost:3000](http://localhost:3000)

## 🧩 Features

- User Registration & Login
- JWT-based Authentication
- Account Creation & Management
- Secure Transactions
- Transaction History

Make sure the database is configured before starting backend.
